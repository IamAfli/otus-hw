apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: iamafli-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: arch.homework
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: iamafli-service
                port:
                  number: 80
